// When iterating over the elements of an array without using the index variable,
// the 'for-of' loop provides a cleaner and more concise syntax.

let numbers = [1, 2, 3, 4, 5, 6, 7];

for (const value of numbers) {
    console.log(value);
}